type A :struct {
	i: i32
}

func A.f() {
	this = nil                 // 禁止这个语句，让编译器提示 implicit 'this' is not assignable
	println(this.i, (*this).i) // 此时对 this 指针的访问生成的指令不再需要做空值检查
}

func main() {
	this: i32 = 123 // this 作为普通变量使用不受影响
	a: A
	a.i = this
	a.f()
}
